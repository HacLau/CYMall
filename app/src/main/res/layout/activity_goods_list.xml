<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data></data>
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:context="com.koudai.borrow.activity.mall.GoodsListActivity">
        <com.lsl.cymall.view.TitleBar
            android:id="@+id/tb_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/title_color"
            app:tbImage="@mipmap/ico_back_white"
            app:tbText=" "
            app:tbTextColor="@color/white"
            app:tbTopPadding="false" />
        <ListView
            android:id="@+id/lv_goods_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/tb_title"
            android:scrollbars="none"/>

    </RelativeLayout>

</layout>